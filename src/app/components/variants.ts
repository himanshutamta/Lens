import { tv } from "tailwind-variants";

export const appContent = tv({
  base: "mx-auto w-[90%] ",
});

export const gradientText = tv({
  base: "bg-gradient-to-r from-[#0091ff] to-[#9cfeff]",
});
